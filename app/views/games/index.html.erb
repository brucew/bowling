<h2>Games</h2>
<div>
  <%= link_to '+New Game', new_game_path, class: 'button' %>

</div>

<div>
  <% @games.each do |game| %>
    <div>
      <%= link_to game_path(game) do %>
        <%= "#{game.created_at.to_s(:short)}: #{game.score}" %>
      <% end %>

    </div>
  <% end %>

</div>