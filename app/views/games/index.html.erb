<h2>Games</h2>
<div>
  <%= button_to '+New Game', games_path %>

</div>

<div>
  <% @games.each do |game| %>
    <div>
      <%= link_to game_path(game) do %>
        <%= "#{game.created_at.to_s(:short)}: #{game.score}" %>
      <% end %>

    </div>
  <% end %>

</div>